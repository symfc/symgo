# symgo

Geometry optimization code under constraint of crystal symmetry.Currently
pypolymlp and mattersim interfaces are provided. The initial code was forked
from `optimization.py` in pypolymlp (see Change Log).
